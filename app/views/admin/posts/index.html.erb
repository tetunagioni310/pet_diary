<div class="container">
  <h2><%= @customer.nick_name %>さんの投稿一覧</h2>
  <div class="row">
    <div class="admin-posts col text-field">
       <table class="col-sm-10 m-auto">
         <tr>
           <th>タイトル</th>
           <th>投稿日</th>
           <th>コメント</th>
           <th>いいね</th>
         </tr>
         
         <% @posts.each do |post| %>
           <tr>
             <td><%= link_to post.post_title.truncate(10), admin_customer_post_path(@customer.id,post.id), class: "btn-links col" %></td>
             <td><%= post.created_at.strftime("%Y/%m/%d") %></td>
             <td><%= post.comments.count %>件</td>
             <td><%= post.likes.count %>件</td>
           </tr>
         <% end %>
       </table>
         
       </ul>
       <%= paginate @posts %>
    </div>
  </div>
</div>