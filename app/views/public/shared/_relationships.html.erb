<% if current_customer.following?(customer) %>
  <%= link_to "フォロー中", public_customer_relationships_path(customer.id), class: "btn-sm btn-links shadow-none", remote: true, method: :delete %>
<% else %>
  <%= link_to "+フォロー", public_customer_relationships_path(customer.id), class: "btn-sm btn-links shadow-none", remote: true, method: :post %>
<% end %>