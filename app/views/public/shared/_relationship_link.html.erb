<% if followings == true %>
  <%= link_to "#{customer.followings.count}フォロー", public_customer_followings_path(customer.id), class: "follow-link" %>
<% else %>
  <%= link_to "#{customer.followers.count}フォロワー", public_customer_followers_path(customer.id), class: "follow-link" %>
<% end %>