<div class="container">
  <div class="row">

    <div class="col-md-8">
      <div class="col text-field">
        <%= image_tag '24971705_m.jpg', class: "col my-3" %>

        <div class="col mt-5">
          <div class="col mx-auto h2 bold text-center border-bottom">新着投稿</div>
          <% if @posts.count >= 1 %>
            <% @posts.each do |post| %>
              <div class="col-sm-8 text-field top-content">
                <div class="col row user-info">
                  <%= image_tag post.customer.get_customer_image(50,50) %>
                  <%= link_to post.customer.nick_name, public_customer_path(post.customer.id), class: "other-link mx-2" %>
                  <div class="small text-muted">
                    <%= time_ago_in_words(post.created_at).upcase %>
                  </div>
                </div>

                <%= link_to public_post_path(post.id), class: "other-link p-0 m-0" do %>
                  <ul class="m-3">
                    <li><%= post.post_title.truncate(10) %></li>
                    <li><%= post.post_content.truncate(30) %></li>
                    <li><%= image_tag post.get_post_image(500,500) , width: "100%", class: "border-radius mt-3" %></li>
                  </ul>
                <% end %>
              </div>
            <% end %>
          <% else %>
            <div class="col-md-8 m-auto text-center">投稿はまだありません</div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-md-4">
        <div class="col text-field">
          <div class="col-lg-8 mx-auto text-center h4 bold mb-3 border-bottom">投稿画像</div>
          <div class="row">
            <% if @random_posts.count >= 1 %>
              <% @random_posts.each do |random_post| %>
                <% if random_post.post_image.attached? %>
                <div class="col-6">
                  <%= link_to public_post_path(random_post.id) do %>
                    <%= image_tag random_post.post_image.variant( resize: "1000x1000", gravity: :center, crop: "1000x800+0+0" ).processed, width: "100px", height: "100px", maxheight: "100px", class: "object-fit-img col px-0 py-md-1 mx-auto" %>
                  <% end %>
                </div>
                <% end %>
              <% end %>
            <% else %>
              <div class="col-8 m-auto text-center">投稿はまだありません</div>
            <% end %>
          </div>
        </div>
      </div>
  </div>
</div>