<div class="container">
  <div class="row">
    <div class="col login-page">
      <div class="h2 mb-5">ログイン</div>
      <%= form_with model: @customer, url: customer_session_path, method: :post do |f| %>
      <%= render "public/shared/error_messages", resource: @customer %>


        <div class="h4 mb-5">会員の方はこちらからログイン</div>
        <div class="col-md-6">
          <div class="form-group row">
            <%= f.label :email, "メールアドレス", class: "col-form-label col-4" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control col-8" %>
          </div>

          <div class="form-group row">

            <%= f.label :password, "パスワード", class: "col-form-label col-4" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control col-8" %>
          </div>

          <div class="form-group row">
            <div class="col-4"></div>
            <% if devise_mapping.rememberable? %>
            <div class="col-8 px-0">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
            <% end %>
          </div>

          <div class="form-group row">
            <div class="col-4"></div>
            <div class="col-8 px-0">
              <%= f.submit "ログイン", class: "btn btn-success col-6" %>
            </div>
          </div>

        </div>
      <% end %>

      <div class="pt-5">
        <div class="h4 mb-3">登録がお済みでない方</div>
        <p><%= link_to "こちら", new_customer_registration_path, class: "btn btn-success mr-3" %>から新規登録を行なってください。</p>
      </div>

      <div class="pt-5">
        <div class="h4 mb-3">管理者ログイン</div>
        <p><%= link_to "こちら", new_admin_session_path, class: "btn btn-success mr-3" %>へ</p>
      </div>

      <div class="pt-5">
        <div class="h4 mb-3">ゲストログイン（閲覧用）</div>
        <p><%= link_to 'こちら', public_guest_sign_in_path, method: :post, class: "btn btn-success mr-3" %>へ</p>
      </div>
    </div>
  </div>
</div>
